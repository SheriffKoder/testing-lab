/**
 * @file entities/invoice/lib/index.ts
 * Public barrel for invoice entity display helpers (lib layer).
 *
 * Used in: views/features that format invoice fields for the UI.
 * Used for: stable import path `@/entities/invoice/lib` without deep imports.
 *
 * Function index:
 * - formatInvoiceStatus (format-invoice-status)
 */

export { formatInvoiceStatus } from "./format-invoice-status";
